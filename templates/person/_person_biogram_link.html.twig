{{ volume.fullcitation }}
{% for p in pages %}
    {% if p.isbio %}<span class="text-nowrap">S. <strong>{{ p.page }} </strong></span><a class="btn btn-danger btn-sm" role="button" href="{{ url_gs_bios~volume.gsvolumenr~'/'~p.page }}" target="_blank" rel="noopener"><strong>Biogramm online lesen</strong></a>
    {% else %}
	<span class="text-nowrap">S. {{ p.page }}</span>
    {% endif %}{%if not loop.last %},{% endif %}
{% endfor %}
