{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="container-fluid">
	<div class="row ms-2 mb-3 col-12">
	    <span class="topic-title">{{ bishop_title }} <a href="{{ path('reference_list', { 'itemTypeId': 1}) }}"><span class="text-nowrap">(nach Erwin Gatz)</span></a></span>
	    <span class="topic-title">Neue Einträge</span>
	</div>
	{% if count > 0 %}
	    <div data-controller="submit-form"
		 data-submit-form-new-entry-value="{{ newEntry }}"
		 data-hide-on-input-target="result">
		{{ include('edit_bishop/_list.html.twig') }}
	    </div>
	{% endif %}
    </div>
{% endblock %}
