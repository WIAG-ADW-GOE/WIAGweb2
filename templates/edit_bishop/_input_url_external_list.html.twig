{{ include('edit_bishop/_input_url_external_essential.html.twig') }}
<div data-controller="insert"
     data-insert-url-value="{{ path('edit_bishop_new_urlexternal', {
			    base_id: base_id,
			    base_input_name: base_input_name
			    }) }}"
     data-insert-hide-trigger-value="yes"
     data-insert-point-value="last">
    {% for url_ext in person.item.urlexternalnoncore %}
	{{ include('edit_bishop/_input_url_external.html.twig', {
	    urlext: url_ext,
	    base_id: base_id,
	    base_input_name: base_input_name,
	    current_idx: loop.index0,
	    is_last: loop.last,
	}) }}
    {% endfor %}
</div>
{% for error_obj in person.inputError|filter(v => v.section == 'external id') %}
    <div class="row">
	<div class="col-12 text-danger">{{ error_obj.msg }}</div>
    </div>
{% endfor %}
